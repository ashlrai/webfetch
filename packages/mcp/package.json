{
  "name": "@webfetch/mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol server exposing webfetch license-first image search to any MCP-speaking agent.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "webfetch-mcp": "./dist/index.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "keywords": [
    "webfetch",
    "mcp",
    "model-context-protocol",
    "image-search",
    "licensing",
    "claude",
    "cursor",
    "agent"
  ],
  "homepage": "https://getwebfetch.com",
  "bugs": {
    "url": "https://github.com/ashlrai/webfetch/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ashlrai/webfetch.git",
    "directory": "packages/mcp"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun run build:clean && bun run build:js && bun run build:fixup",
    "build:clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build:js": "bun build src/index.ts --target=node --format=esm --outfile=dist/index.js --external=sharp --external=@modelcontextprotocol/sdk --external=@webfetch/core --external=zod",
    "build:fixup": "node -e \"const fs=require('node:fs'),path=require('node:path');if(fs.existsSync('dist/index.js')){const s=fs.readFileSync('dist/index.js','utf8');fs.writeFileSync('dist/index.js',s.replace(/^#!\\/usr\\/bin\\/env bun/m,'#!/usr/bin/env node'));}const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(p.endsWith('.d.ts')){const s=fs.readFileSync(p,'utf8').replaceAll('.ts\\\"','.js\\\"');fs.writeFileSync(p,s);}}};if(fs.existsSync('dist'))walk('dist');\"",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@webfetch/core": "0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
